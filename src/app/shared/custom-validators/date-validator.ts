// import { FormControl } from '@angular/forms';

// export function validDateValidate(c: FormControl) {
//   const INVALID = { invalidDate: true };
//   if (!c) {
//     return INVALID;
//   }

//   if (!c.value && !(c.value && c.value.trim().length)) return null;

//   const date = moment(c.value, DATE_FORMAT, true);
//   return date.isValid() ? null : INVALID;
// }
